---
description: Interactive Q&A / quiz over docs/guide/ to build fluency to present the project. Default is a Socratic quiz (graduated, graded, always ending with the sayable version); an optional argument scopes to a doc (00–06) or topic; say "office hours" to flip to asking questions instead.
argument-hint: [doc 00–06 | topic e.g. retrieval, agent, eval, firewall | empty = progressive mix]
---

Follow the instructions in `.agent/commands/quiz.md`.

The user may provide a doc number (00–06) or a topic keyword as an argument: $ARGUMENTS.
If $ARGUMENTS is empty, run a progressive mix starting at the spine (00 overview, 01
data foundation).
