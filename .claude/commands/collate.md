---
description: Collate docs/presentation/gathered-context.md into a presentation-ready summary at docs/presentation/summary.md — synthesized (not concatenated), insight-first, with a slide-by-slide outline and time budget for a talk of ≤ 40 minutes. Optional argument overrides length or focus (e.g. "25 min", "focus on evaluation").
argument-hint: [optional length/focus e.g. "30 min" | "focus on retrieval" | empty]
---

Follow the instructions in `.agent/commands/collate.md`.

An optional length or focus override may be provided as an argument: $ARGUMENTS.
If $ARGUMENTS is empty, target ≤ 40 minutes and cover the gathered themes proportionally.
