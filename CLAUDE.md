See AGENTS.md for project memory and the SDD workflow.

This file is a thin shim so Claude Code picks up project context. The
canonical instructions live in `AGENTS.md`, the law in
`docs/constitution.md`, the map in `docs/architecture.md`, and the module
tracker in `docs/roadmap.md`.
